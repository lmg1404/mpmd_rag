name: 'Continuous Delivery (CD)'

on:
    push:
        branches:
            - main

jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
          - name: Transfer files to VM
            run: echo "Deployment to VM will go here!"
          - name: Run deployment on VM
            run: echo "Run everything on the VM"